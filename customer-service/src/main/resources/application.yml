server:
  port: 9413

spring:
  application:
    name: customer-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
